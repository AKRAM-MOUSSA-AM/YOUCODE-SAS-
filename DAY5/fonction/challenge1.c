#include <stdio.h>

// Create a function
void calculateSum() {

}

int main() {
  calculateSum();  

printf("entre x and y: ");
    int x;
    int y;
    scanf("%d %d", &x, &y);
    int sum = x + y;

    printf("The sum of x + y is: %d", sum);

  return 0;
}